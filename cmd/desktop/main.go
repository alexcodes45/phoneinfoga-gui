package main

// NOTE: After you generate a Wails frontend, replace this with actual Wails bootstrap.
// This stub compiles standalone for now (no GUI), so `go run ./cmd/desktop` prints a message.
// To run the GUI, install Wails and use `wails dev` once frontend is present.

import "fmt"

func main() {
	fmt.Println("PhoneInfoga Desktop bootstrap. Run `wails init` to create the UI shell, then `wails dev`.")
}
